RETHINK GRAMMAR

grammar_reader:
    1) return hashmap instead of vector (?)

lexical_analyzer:
    1) output vector of commands for parser to interpret 
        - if order of commands is correct
        - if command is valid