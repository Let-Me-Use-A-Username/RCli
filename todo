Project:
    -Todo:Logger to log output
        -parser operations
    -Note:Consider wether a global object is needed to keep the current working 
          directory, current and last operation

RCLi:
    -Todo:Change big if statemets to patterns
    -Todo:Handle errors better in general
        -Example:change the unwraping of UserInput
    -Todo:Reformat when the parser works to a certain acceptable degree


parser:
    -Note:consider whether matching commands is better than tree implementation


invoker:
    -Todo:Create all the basic operations
    -Note:If file not found and dir not found then dir is current working directory(?)
    -Todo:create a thread per operation