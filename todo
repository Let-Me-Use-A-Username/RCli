General:
    -Change big if statemets to patterns
    -Handle errors
    -reformat whence the parser works to a certain acceptable degree

lexical_analyzer:
    -Output vector of commands for parser to interpret 
        -If order of commands is correct

parser:
    -Validate command